_site/
.jekyll-cache/
.jekyll-metadata
.bundle/
vendor/

# clasp / GAS production deployment (never commit credentials or project IDs)
.clasprc.json
.clasp.json
.gas-deploy/

# Node dependencies (e.g. cloud-run/stripe-webhook-relay/). Never commit secrets/env files.
node_modules/
.env
.env.*
